class ResultsController {
  constructor ($rootScope) {
    'ngInject';

    // for (let i = 0; i < $rootScope.parts.length; i++){
    // 	for (let j = 0; j < $rootScope.parts[i].images.length; j++){
    // 		if ($rootScope.parts[i].images[j].sort === 'a' && $rootScope.parts[i].images[j].size === 'Tall'){
    // 			$rootScope.parts[i].smallImage = $rootScope.parts[i].images[j].path.Scheme + '://' + $rootScope.parts[i].images[j].path.Host + $rootScope.parts[i].images[j].path.Path;
    // 			continue;
    // 		}
    // 	}
    // }
    
  }
}

export default ResultsController;
